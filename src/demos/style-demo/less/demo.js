import "./index.less"

export default function LessDemo() {
  return (
    <div className="less-demo">
      安装less，less-loader，并使用"customize-cra"
      、"react-app-rewired"改写cra默认配置，以支持less文件编写样式。
      PS:这行文字的样式就是使用less编写。
    </div>
  )
}
